version: 2

sources:
  - name: raw
    database: jaffle_shop
    schema: raw
    description: Backoffice data from Deel platform
    tables:
      - name: organizations
        description: One record per corporate organization with an active contract
      - name: invoices
        description: One record per generated invoice for services
